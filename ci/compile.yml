---
platform: linux

image: docker://docker.ci.eng.pivotal.io/libhdfs3-toolchain-image
inputs:
  - name: libhdfs3_src
params:
  OUTPUT_PATH: build
run:
  path: libhdfs3_src/ci/compile.bash
outputs:
  - path: build
    name: libhdfs3_build_dir
